import 'package:flutter/material.dart';
import 'package:volcano/uses/components/body.dart';

class Exercises extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ExercisesBody(),
    );
  }
}
